[package]
name = "clock"
version = "1.0.2"
edition = "2024"

[dependencies]
clap      = { version = "4", features = ["derive"] }
chrono    = { version = "0.4", features = ["clock"] }
crossterm = "0.28"
